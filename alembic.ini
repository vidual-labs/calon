# Alembic configuration, for authoring migrations during development.
#
# The application does not read this file: it runs migrations at startup through
# calon.migrate.upgrade_to_head, which builds its own config so that a container holding
# only the installed wheel can still upgrade its database.
#
# Create a migration with:
#   uv run alembic revision --autogenerate -m "short description"
#
# The URL below is only the one autogenerate compares against. Override it with
# CALON_DB_PATH, or with `-x` on the command line, to point somewhere else.

[alembic]
script_location = src/calon/migrations
prepend_sys_path = src
path_separator = os
file_template = %%(rev)s_%%(slug)s

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
