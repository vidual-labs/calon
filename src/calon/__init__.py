"""calon — a lean, self-hostable booking intake tool.

calon captures booking requests, applies operator-defined scheduling rules, checks
availability, and produces a generic calendar handoff that works with any major calendar.

This is the single source of truth for the project version; ``pyproject.toml`` reads it.
"""

__version__ = "0.1.0.dev0"

__all__ = ["__version__"]
